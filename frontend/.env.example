# Copy this to frontend/.env.local for local development
VITE_API_URL=http://localhost:8000

# Set to "true" to show Google / Apple SSO buttons (requires Auth0 configured in backend)
VITE_SSO_ENABLED=false
